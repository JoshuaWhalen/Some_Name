///setStats(obj_player id, damage, max_speed, max_hit, recovery, attack_speed, gravity, hooked_velocity, jump)
with(argument0){
    damage = argument1;
    max_speed = argument2;
    max_hit = argument3;
    recovery = argument4;
    attack_speed = argument5;
    gravy = argument6;
    hooked_velocity = argument7;
    jump = argument8;
}
